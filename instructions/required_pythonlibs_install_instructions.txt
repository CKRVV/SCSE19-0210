pip install beautifulsoup4
pip install requests
pip install pymongo
pip install easygui
pip install numpy
pip install genism
pip install pyLDAvis
pip install spacy
python3 -m spacy download en

To run the webscraper.py and conert.py script, can be run with the python command or through the terminal console.

To run the mergeSchema, mongo and regression folders, run with python command, python _____

To run the nlp folder, run with python command, python3 nlp



